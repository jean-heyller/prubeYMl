name: "My action"
description: "My first action"

inputs:
  message:
    description: "The message to print"
    required: true
    default: "Hello World"


runs:
  using: "composite"

  steps:
    - name: Hola Mundo
      run: echo "Print ${{ inputs.message }}"
      shell: bash
