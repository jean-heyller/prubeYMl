name: Test

on: [push]
  
jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - name: ls
        run: | #pipe para generar comandos multilinea
          echo "Hello, World" >> hello.txt
          cat hello.txt